# Session pickup — 2026-06-29 (Mini take-over)

Today's hand-off card from Laptop-Claude-Code (on the VM) to Mini-Claude-Code (you, reading this on the Mac Mini). This is *in addition to* [`CONTEXT.md`](CONTEXT.md) — read that first for the full migration plan; this doc is the delta + the auto-memory bridge.

---

## What happened today on the VM (2026-06-29)

After a 7-week break, Dave restarted the migration. In a single short laptop session:

1. Pushed the 2 pending commits that had been sitting unpushed since 2026-05-10: the migration handoff/bootstrap docs + this `mac-mini-migration` project dir.
2. Committed a small `cron/jobs.json` change — unicode escape normalization + an empty `"state": {}` openclaw added during prior runs. Commit `0acdc66`. Origin is now caller-clean.
3. Confirmed with Dave: Mini is in hand and ready, he wants Phase A driven *on the Mini* directly (not relayed back through the VM).
4. Wrote this doc so the next session — you, on the Mini — picks up without re-narrating.

**Today's commits on `origin/main` (top of `git log`):**
- `0acdc66` cron/jobs.json: normalize escapes + add empty state to car-search
- `e48b39e` mac-mini-migration: add CLONE.md cheat sheet
- `362555a` workspace: HIERARCHY.md + mac-mini-migration project (Option B plan)
- `77e821f` handoff: migration context + bootstrap card for Mac Mini

The VM is still running. Bishop still serves Dave from it. We have not started cutover yet.

---

## Auto-memory bridge

Auto-memory is laptop-local (`~/.claude/projects/-Users-bishop--openclaw/memory/`) and does NOT travel via `git clone`. The Mini has none of this. Here are the load-bearing pieces — recreate them organically as conversations validate them; don't dump them all into MEMORY.md verbatim.

### User profile (recreate as `user_dave.md` when natural)
- **Dave Otte**, 54, Kirkland WA. Technical Art Manager → IC at Meta (research org). RSU cliff Q1 2027. Python primary, PyQt5/SQLite/ONNX, macOS.
- Communication: direct, warm, no pleasantries. Solution-first. Enneagram 4w3, Jungian therapy. Aesthetic: Warm Modern Heritage.
- Email: `otte.dave@gmail.com`. Phone: `+1-650-823-9528` (this is the *delivery destination* in cron jobs; not Bishop's source identity).

### Feedback — Dave's working preferences (recreate as memory files as triggered)
- **Autonomy for bounded reversible work** — don't gate per step on "ok to continue?" for known-cost, reversible work. State the cost ceiling when relevant. Pause only on real failures, destructive/irreversible actions, content-questions only Dave can answer, or scope changes.
- **Spec planning ≠ dispatch** — when Dave plans a skill at the laptop, your job ends at writing the spec to `~/.openclaw/specs/<skill-name>.md`. Bishop dispatches via skills-agent. Don't pre-empt.
- **Code changes via skills-agent only** — no in-process Bishop edits to skill files, even one-line "obviously correct" patches. Trust model is structural: Bishop is Haiku (orchestrator), skills-agent is a stronger model (coder). The PreToolUse hook at `workspace/.claude/hooks/skill-edit-guard.py` enforces this on Bishop's session.
- **Compose-first applies to us, not just to agents** — before building any new orchestration/dispatch/monitoring infra for openclaw, survey bundled skills at `/opt/homebrew/lib/node_modules/openclaw/skills/` and extension skills under `dist/extensions/*/skills/`. Survey before proposing infrastructure.

### Project memory — what's loaded and ready (recreate as memory files as triggered)
- **Skill-builder & skills-agent** — Phases 1-5 shipped (build/patch methodology, agent registration, dispatch pattern). Phase 6 shipped 2026-05-04 evening: PreToolUse hook + iteration-mode + install-with-preview pattern.
- **YNAB skill** — built, in preview mode (cron payload uses `--no-apply`). Dave reviewed a preview digest on 2026-05-04; he hasn't said "go" yet. **Don't enable live mode during migration.** Preserve preview state. Live-enable is a separate Dave decision.
- **Patch-mode dispatch not yet empirically validated.** Bishop violated the "no in-session edits" rule twice on 2026-05-04 before the hook shipped. The PreToolUse hook now forces dispatch on the next patch. That validation is still pending.
- **Job-search skill** — Phase 1+2 shipped 2026-05-02/03. Layer 1 ATS + Layer 2 web_search. Weekly cadence, ~$2.50-3/mo. Uses `launchd`, not openclaw cron. Plist at `workspace/skills/job-search/scripts/install.sh`. **On the Mini: needs fresh launchd install via that script.**
- **Active follow-up still open from May:** job-search launchd schedule installation. Defer until the Mini's openclaw is stable.

---

## Where you are right now in the migration

You're at the start of Phase A per [`CONTEXT.md`](CONTEXT.md). Dave is at the Mini's keyboard. Likely his very first prompt to you was a variation of "Load project mac-mini-migration." Phase A is foundations (brew, node, claude, op, tailscale, gh, Apple ID/iMessage sign-in). Most of these need to be done at the Mini's GUI — the bootstrap card in `handoff/BOOTSTRAP.md` is Option-A vintage but the install commands themselves still apply. The Option-B phase ordering is in `CONTEXT.md` § "Mini install procedure."

**Don't worry about the `handoff/` directory** — it's superseded by this `projects/mac-mini-migration/` dir. Leave it; it gets deleted in Phase H per `CONTEXT.md`.

### Phase A checklist (foundations on the Mini)

Verify what's already done (Dave may have ticked several off during the 7-week break or during today's session before opening Claude Code):

- [ ] Homebrew installed (`brew --version`)
- [ ] `brew install node git gh 1password-cli`
- [ ] `brew install --cask tailscale`
- [ ] `npm install -g @anthropic-ai/claude-code` and `claude --version`
- [ ] Claude Code authed against Anthropic account (already done — you're running)
- [ ] Apple ID signed in on Mini as **Bishop's** Apple ID (NOT Dave's). System Settings → Apple ID → iMessage on for `+1-425-436-8004`.
- [ ] Tailscale enrolled, Mini visible in tailnet
- [ ] `op signin` against Dave's 1Password account
- [ ] `gh auth login -h github.com` as `bishop-wayland` (confirm with `gh auth status`)
- [ ] `git clone https://github.com/bishop-wayland/openclaw-home.git /tmp/openclaw-source` (the scratch source; NOT into `~/.openclaw/`)

When Phase A is green, move to Phase B (`openclaw onboard`).

---

## Identity prerequisites — confirm with Dave before Phase A.iMessage

Critical: Bishop's identities need to actually exist before they can be signed into. From the original identity plan:

- **Apple ID for Bishop** — created? If not, this blocks A.Apple ID step. Apple ID creation needs a verification phone number; the +1-425-436-8004 should work if it's already provisioned.
- **Phone number `+1-425-436-8004`** — provisioned (carrier or eSIM)? If not, this blocks both iMessage and Apple ID flows.
- **Gmail `yutani.w.bishop@gmail.com`** — created? Doesn't block Phase A but blocks Phase D (Gmail webhook).
- **GitHub `bishop-wayland`** — confirmed working (the repo you may have just cloned is owned by this account).

If any of these aren't ready, surface to Dave immediately. Don't drive past them.

---

## Open questions still pending from CONTEXT.md (surface to Dave early)

These never got answered before the May break. Ask before doing Phase D-onward:

1. **GCP Pub/Sub** — new topic+subscription for `yutani.w.bishop@gmail.com`, or repoint the existing subscription? Repoint is faster; fresh is cleaner.
2. **Tailscale Funnel URL** — same URL as the VM (re-point at Mini node) or new URL (re-point GCP subscription)?
3. **Old `bishopunit937@gmail.com`** — abandoned or kept as a backup inbox? Affects AGENTS.md Rule 3a trusted-senders list.
4. **`workspace/architecture.md`** — rewrite from scratch on the Mini against the documented openclaw model, or skip and rely on HIERARCHY.md + openclaw's own docs?

---

## Tone reminders

Dave reads diffs and logs. Don't summarize what he can already see. One-sentence end-of-turn updates. Push back when you disagree — passive agreement annoys him. When something works, say so. When something's broken, say so directly. Recommendation-first for exploratory questions.

For Phase A: most steps are commands he runs at the Mini's keyboard. Be a check-the-output partner, not a hand-holder. He's a 54-year-old Python dev who has been driving openclaw for months.

---

## What to do right now

1. Confirm with Dave: which Phase A items are already done? (so you don't waste turns re-running brew install on a fresh box)
2. Walk the rest of Phase A as a checklist, checking outputs as you go.
3. Stop at the start of Phase B (`openclaw onboard`) and confirm with Dave before the fresh openclaw install — that's the first non-reversible-ish moment (the install writes to `~/.openclaw/` which is the runtime home; bad install means cleanup).
4. After Phase B, work through the cherry-pick (`CONTEXT.md` Phase C-D) deliberately. Read each file before copying. The cleanup is the whole point of Option B.

---

## Reach back to Laptop-Claude-Code?

You can't directly — the laptop session and this Mini session are separate processes. If you discover state I forgot to capture in this doc, just ask Dave; he'll either remember or open a fresh session on the VM to retrieve. Worst-case escape valve: he can pull a file from the VM via scp/airdrop and you read it.

When the migration is complete and the VM is being decommissioned, the laptop session's prep work is over. There's no return trip planned.

Welcome to the Mini. Time to bring Bishop home for real this time.
